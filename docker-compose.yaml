services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: trends-bird-api
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env